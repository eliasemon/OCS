version: '3.8'

services:
  winsetup-test:
    build:
      context: .
      dockerfile: Dockerfile.test
    container_name: winsetup-test
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/api/packages', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s
    networks:
      - winsetup-test-net

networks:
  winsetup-test-net:
    driver: bridge
