version: '3.8'

services:
  installora-test:
    build:
      context: .
      dockerfile: Dockerfile.test
    container_name: installora-test
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/api/packages', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s
    networks:
      - installora-test-net

networks:
  installora-test-net:
    driver: bridge
